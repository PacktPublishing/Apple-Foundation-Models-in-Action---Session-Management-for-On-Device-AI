//
//  SessionTranscriptExample.swift
//  FoundationModelCourse2
//
//  Created by DevTechie on 10/14/25.
//

import Foundation
import FoundationModels
import Playgrounds

/*
 
 // MARK: Session transcript example
 
#Playground {
    let instructions = "You are a friendly spanish teacher; give translations, and one example sentence in Spanish"
    let session = LanguageModelSession(instructions: instructions)
    //let generationOptions = GenerationOptions(temperature: 0.5)
    //let reponse = try await session.respond(to: "see you tomorrow", options: generationOptions)
    let reponse = try await session.respond(to: "see you tomorrow")
    print(reponse.content)
    print(session.transcript)
}

*/
